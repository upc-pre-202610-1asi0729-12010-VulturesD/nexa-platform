# v1.0.2 - OpenAPI metadata correction

## Changes

- Updated the public OpenAPI version from the stale `0.6.1` value to `1.0.2`.
- Removed the inaccurate `Academic use` license declaration because the repository does not declare a software license.

## Validation

- `./mvnw test`
- `./mvnw package`
- Production `/v3/api-docs` verification after deployment
